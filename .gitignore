# most ignored files are just for testing/notes, and are not actually projects...
# or they are incomplete to the point where they don't yet serve their purpose...
# or they are temporary, not original (ie jQuery.js), a joke (ie 3quine.html), or something else
# executables and object files are ignored because I didn't write them, the compiler did

/C#
/C++
/Java
/PowerShell
/txt
/TypeScript
/Haskell
/Graphviz

HTML/3quine.html
HTML/calculator.html
HTML/BlackJack.html

JavaScript/jquery.js


# not actually passwords.
Python/passwords.py

notes.*
test.*
testing.*
*.exe
*.pdf
*.o
